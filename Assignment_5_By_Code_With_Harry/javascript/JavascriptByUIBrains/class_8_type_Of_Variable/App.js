//String Data Type
let employeeName="pratik";
//let keyword to declare the variable and assigned to a value
console.log(`value:${employeeName} :Type: ${typeof employeeName}`)
//providing the template String By Back Tick Symbol and dynamic value inside the template String with ${}


//Number Data Type
let employeeAge=40;
//let keyword to declare the variable and assigned to a value
console.log(`value:${employeeAge} :Type: ${typeof employeeAge}`)
//providing the template String By Back Tick Symbol and dynamic value inside the template String with ${}


//Boolean Data Type
let isManager=true;//true is in small letter
//let keyword to declare the variable and assigned to a value
console.log(`value:${isManager} :Type: ${typeof isManager}`)
//providing the template String By Back Tick Symbol and dynamic value inside the template String with ${}

//undefined Data Type
let name;//declaring the Variable but Not assign ing value to it
//let keyword to declare the variable
console.log(`value:${name} :Type: ${typeof name}`)
//typeOf Undefined ===> undefined

//Null Data Type:-also known as Dummy Value
let name2=null;
//let keyword to declare the variable but Not assign ing value to it
console.log(`value:${name2} :Type: ${typeof name2}`)
//type of null-->Object

//reassigning is possible in javascript:-
let name1;//declaring the Variable but Not assign ing value to it
//let keyword to declare the variable
console.log(`value:${name1} :Type: ${typeof name1}`)
name1=10//assigning new Value with to the variable
console.log(`value:${name1} :Type: ${typeof name1}`)
name1="Abi"//assigning new Value with to the variable
console.log(`value:${name1} :Type: ${typeof name1}`)
